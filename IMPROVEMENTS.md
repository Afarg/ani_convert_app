# 現状の課題と改善計画

ポートフォリオ・実運用の両観点で見つかった課題を、優先度順にまとめる。各項目は「課題 → 影響 → 修正法」の形式で記載する。

## 優先度: 高

### 1. アニメーションのBefore/After（GIF）が無い

- **課題**: このツールは「まばたき」「歩行」という動きそのものが成果物なのに、READMEには静止画すら無く、動きが一切伝わらない。
- **影響**: 差分生成ツールという性質上、静止画のREADMEでは価値がほぼ伝わらない。ポートフォリオとして最も損している部分。
- **修正法**: `scripts/verify_animation_output/` に既に `front.png` / `front_blink.png` / `diagonal.png` / `diagonal_blink.png` / `diagonal_walk2.png` 等の差分画像がある。これらをffmpeg等でアニメーションGIFに変換し（例: `front.png`→`front_blink.png` のまばたきループ、`front.png`→`diagonal_walk2.png` の歩行ループ）、READMEに埋め込む。

### 2. フェーズ2・3（斜め・横向き・後ろ向き）が未検証

- **課題**: `docs/design/00-overview.md` に「フェーズ2・3は未検証（img_to_pixcel_app側の複数アングル入力UIが未実装のため）」と明記されている。
- **影響**: 「どこまで動くか」の境界が README 側から見えないと、利用者が不用意にフェーズ2・3を試して失敗する可能性がある。
- **修正法**: GitHub Issueとして「フェーズ2・3の検証」を明示的に起票し、README/IMPROVEMENTS双方から参照する。`img_to_pixcel_app` 側の複数アングルUIが実装され次第、テストバンドルを生成して検証する。

### 3. 自動テスト・CIが無い

- **課題**: `scripts/test_bundle.py` は手動実行前提のスクリプトであり、pytestベースの自動テストやCIが無い。
- **影響**: まばたき位置・歩行フレームの座標計算にリグレッションが起きても検知できない。
- **修正法**: `pytest` を導入し、`scripts/business_character_a` 等の既存フィクスチャを使って「目の位置が正しいマスに変換されているか」「歩行フレームの脚部座標が想定通り動いているか」を検証するテストを追加し、GitHub Actionsで自動実行する。

## 優先度: 中

### 4. `models/` ディレクトリが空のまま

- **課題**: リポジトリに `models/` フォルダが存在するが中身が空。
- **影響**: 「未完成のまま公開した」という印象を与えかねない。
- **修正法**: 現時点で機械学習モデルを使っていないなら削除する。将来的にML的な目検出等を計画しているなら、README/docsにその意図を一言残す。

### 5. LICENSEファイルが無い

- **課題**: リポジトリにライセンス表記が無い。
- **修正法**: MITライセンス等をリポジトリルートに追加する。

### 6. コミット履歴が1コミットのみ

- **課題**: 開発の全過程を1コミットにまとめて公開したため、設計→実装→検証の過程が見えない。
- **修正法**: 今後の機能追加・バグ修正は小さい単位でコミットする。
